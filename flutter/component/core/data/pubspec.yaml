name: core_component_data
description: Core Component Data

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  core_component_domain:
    path: ../domain

  injectable: ^2.5.0
  get_it: ^8.0.0
  dartz: ^0.10.1

  dio: ^5.7.0
  retrofit: ^4.4.1
  jiffy: ^6.3.1
  json_annotation: ^4.9.0
  json_serializable: ^6.8.0
  drift:
  drift_dev: ^2.20.3
  drift_flutter: ^0.2.0
  shared_preferences: ^2.3.2
  flutter_secure_storage: ^9.2.2

dev_dependencies:
  flutter_lints:
  build_runner:
  injectable_generator:
  retrofit_generator:
