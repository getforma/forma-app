name: core_component_domain
description: Core Component Domain

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  injectable: ^2.3.2
  get_it: ^7.6.4
  dartz: ^0.10.1
  freezed_annotation: ^2.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  build_runner:
  injectable_generator:
  freezed: ^2.4.1

flutter:
