name: sensor_component_data
description: Sensor Component Data

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  sensor_component_domain:
    path: ../domain

  session_component_domain:
    path: ../../session/domain

  core_component_data:
    path: ../../core/data

  injectable: ^2.5.0
  get_it: ^8.0.0
  dartz: ^0.10.1
  drift:
  drift_dev: ^2.20.3
  drift_flutter: ^0.2.0

  pigeon: ^22.4.2
#  path_provider: ^2.1.4

dev_dependencies:
  flutter_lints:
  build_runner:
  injectable_generator:
