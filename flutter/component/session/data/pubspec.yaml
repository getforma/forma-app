name: session_component_data
description: Session Component Data

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  forma_app:
    path: ../../../../

  session_component_domain:
    path: ../domain

  sensor_component_domain:
    path: ../../sensor/domain

  core_component_domain:
    path: ../../core/domain

  core_component_data:
    path: ../../core/data

  sensor_component_data:
    path: ../../sensor/data

  injectable: ^2.5.0
  get_it: ^8.0.0
  dartz: ^0.10.1

  dio: ^5.7.0
  retrofit: ^4.4.1
  jiffy: ^6.3.1
  json_annotation: ^4.9.0
  json_serializable: ^6.8.0

  device_info_plus: ^11.0.0
  android_id: ^0.4.0
  geolocator: ^13.0.1

  flutter_background_service: ^5.0.10

dev_dependencies:
  flutter_lints:
  build_runner:
  injectable_generator:
  retrofit_generator:

#dependency_overrides:
#  dart_style: ^2.3.7
