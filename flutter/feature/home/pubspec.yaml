name: home_feature
description: Home Feature

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  forma_app:
    path: ../../../

  core_feature:
    path: ../core

  sensor_component_domain:
    path: ../../component/sensor/domain

  session_component_domain:
    path: ../../component/session/domain

  questionnaire_component_domain:
    path: ../../component/questionnaire/domain

  recommendation_component_domain:
    path: ../../component/recommendation/domain

  cupertino_icons: ^1.0.8
  flutter_svg: ^2.0.10+1
  collection: ^1.18.0
  flutter_bloc: ^8.1.6
  auto_route: ^9.2.2
  injectable: ^2.5.0
  get_it: ^8.0.0
  freezed_annotation: ^2.4.4
  flutter_screenutil: ^5.9.3

dev_dependencies:
  flutter_lints:
  build_runner:
  injectable_generator:
  auto_route_generator:
  freezed: ^2.4.1

flutter:
  uses-material-design: true

  assets:
    - asset/
    - asset/icon/
